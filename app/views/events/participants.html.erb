<%= render 'breadcrumb' %>
<% content_for :breadcrumb do %>
  > <%= link_to @event.name, event_path(@event) %>
  > 報名者
<% end %>
<div class="container">
  <div class="d-flex p-2">
    <%= link_to '返回', event_path(@event), class: 'btn btn-outline-secondary' %>
  </div>
  <%= render 'event_users/table', event_users: @event_users %>
</div>
